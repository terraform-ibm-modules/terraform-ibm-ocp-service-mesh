{{- if (eq .Values.ingress.lbtype "nlb") }}
{{- range $lb_zone, $lb_subnet := $.Values.nlbzonessubnets }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-{{ .Values.ingress.name }}-{{ $lb_zone }}
  namespace: {{ .Values.ingress.namespace }}
spec:
  selector:
    matchLabels:
      {{- if .Values.ingress.istioselectors }}
      {{- toYaml .Values.ingress.istioselectors | nindent 6 }}
      {{- end }}
  template:
    metadata:
      annotations:
        inject.istio.io/templates: gateway
      labels:
        {{- if .Values.ingress.istioselectors }}
        {{- toYaml .Values.ingress.istioselectors | nindent 8 }}
        {{- end }}
        sidecar.istio.io/inject: "true"
    spec:
      replicas: {{ .Values.ingress.replicacount }}
      nodeSelector:
        {{- if .Values.ingress.nlbzones }}
        {{- range $zone := .Values.ingress.nlbzones }}
        ibm-cloud.kubernetes.io/zone: {{ $zone | quote }}
        {{- end }}
        {{- end }}
      {{- if .Values.ingress.affinity }}
      affinity:
      {{- toYaml .Values.ingress.affinity | nindent 6 }}
      {{- end }}
      serviceAccountName: ingress-{{ .Values.ingress.name }}
      serviceAccount: ingress-{{ .Values.ingress.name }}
      {{- if .Values.ingress.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.ingress.terminationGracePeriodSeconds }}
      {{- end }}
      tolerations:
      {{- if .Values.ingress.tolerations }}
      {{- toYaml .Values.ingress.tolerations | nindent 6 }}
      {{- end }}
      containers:
      - name: istio-proxy
        image: auto
        securityContext:
          capabilities:
            drop:
            - ALL
          allowPrivilegeEscalation: false
          privileged: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
        resources:
        {{- if .Values.ingress.resources }}
        {{- toYaml .Values.ingress.resources | nindent 10 }}
        {{- end }}
        ports:
        - containerPort: 15090
          protocol: TCP
          name: http-envoy-prom
        {{- if .Values.ingress.ports }}
        {{- range .Values.ingress.ports }}
        {{- end }}
        - containerPort: {{ .port }}
          protocol: {{ .proto | toString }}
          name: {{ .name }}
        {{- end }}
{{- end }}
{{- end }}
#---
# Allow outside traffic to access the gateway
# This is optional, only needed in case your cluster contains restrictive NetworkPolicies
#apiVersion: networking.k8s.io/v1
#kind: NetworkPolicy
#metadata:
#  name: gatewayingress
#spec:
#  podSelector:
#    matchLabels:
#      istio: ingressgateway
#  ingress:
#    - {}
#  policyTypes:
#  - Ingress
