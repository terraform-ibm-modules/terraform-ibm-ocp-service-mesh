{{- if .Values.egress.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    {{- toYaml .Values.egress.istioselectors | nindent 4 }}
    release: istio
  name: {{ .Values.egress.pdb.name }}
  namespace: {{ .Values.egress.namespace }}
spec:
  {{- if .Values.egress.pdb.minAvailable }}
  minAvailable: {{ .Values.egress.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.egress.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.egress.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- toYaml .Values.egress.istioselectors | nindent 6 }}
      {{- if .Values.egress.extendSelector }}
      deploymentName: {{ .Values.egress.deploymentName }}
      {{- end }}
{{- end }}
